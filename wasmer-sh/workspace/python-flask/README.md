# Flask

```sh
pip install -r requirements.txt
python server.py
```

The preview opens on port 8000. Try `/api/hello?name=Wasmer` and `/health`.
Press Ctrl-C to stop the server. Only Flask and its dependencies are installed.
